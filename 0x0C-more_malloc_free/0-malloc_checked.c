#include "main.h"
#include <stdlib.h>

/**
* malloc_checked - allocates memory
*
* @b: Size of memory will be allocated
*
* Return: void
*/


void *malloc_checked(unsigned int b)
{
	void *m = malloc(b);

	if (ptr == NULL)
	{
		exit(98);
	}

	return (ptr);
}
